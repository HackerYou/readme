import React from 'react';

const Loader = () => {
    return (
        <div className="loader-container">
            <h2>Loading</h2>
            <span className="loader" />
            <span className="loader" />
            <span className="loader" />
            <span className="loader" />
        </div>
    );
};

export default Loader;
